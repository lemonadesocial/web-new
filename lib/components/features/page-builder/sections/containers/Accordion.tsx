'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function AccordionContainer(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Accordion" {...props} />;
}

export function AccordionPanelCanvas(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Accordion Panel Canvas" {...props} />;
}
