'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function Tabs(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Tabs" {...props} />;
}

export function TabCanvas(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Tab Canvas" {...props} />;
}
