'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceFeed(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Feed" {...props} />;
}
