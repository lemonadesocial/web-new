'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceEvents(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Events" {...props} />;
}
