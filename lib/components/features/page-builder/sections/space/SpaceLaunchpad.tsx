'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceLaunchpad(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Launchpad" {...props} />;
}
