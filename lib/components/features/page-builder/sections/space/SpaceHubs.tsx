'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceHubs(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Hubs" {...props} />;
}
