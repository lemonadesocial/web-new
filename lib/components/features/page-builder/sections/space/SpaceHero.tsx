'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceHero(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Hero" {...props} />;
}
