'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceMembers(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Members" {...props} />;
}
