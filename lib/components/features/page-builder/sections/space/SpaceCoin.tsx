'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceCoin(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Coin" {...props} />;
}
