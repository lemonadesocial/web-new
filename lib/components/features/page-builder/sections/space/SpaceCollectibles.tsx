'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SpaceCollectibles(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Space Collectibles" {...props} />;
}
