'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventSponsors(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Sponsors" {...props} />;
}
