'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventCollectibles(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Collectibles" {...props} />;
}
