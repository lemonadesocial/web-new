'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventCountdown(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Countdown" {...props} />;
}
