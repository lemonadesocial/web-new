'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventSchedule(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Schedule" {...props} />;
}
