'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventAbout(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event About" {...props} />;
}
