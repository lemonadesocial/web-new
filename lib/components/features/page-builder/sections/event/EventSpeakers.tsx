'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventSpeakers(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Speakers" {...props} />;
}
