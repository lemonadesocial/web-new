'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventGallery(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Gallery" {...props} />;
}
