'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventHero(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Hero" {...props} />;
}
