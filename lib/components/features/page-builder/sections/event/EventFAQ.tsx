'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventFAQ(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event FAQ" {...props} />;
}
