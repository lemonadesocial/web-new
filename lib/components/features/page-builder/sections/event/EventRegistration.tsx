'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function EventRegistration(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Event Registration" {...props} />;
}
