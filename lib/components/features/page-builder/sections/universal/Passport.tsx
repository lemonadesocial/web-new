'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function Passport(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Passport" {...props} />;
}
