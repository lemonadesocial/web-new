'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function Header(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Header" {...props} />;
}
