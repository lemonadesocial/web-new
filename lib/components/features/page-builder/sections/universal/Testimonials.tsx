'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function Testimonials(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Testimonials" {...props} />;
}
