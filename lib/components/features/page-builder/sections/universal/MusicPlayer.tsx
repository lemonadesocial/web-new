'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function MusicPlayer(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Music Player" {...props} />;
}
