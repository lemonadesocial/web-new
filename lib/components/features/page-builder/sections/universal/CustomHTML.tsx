'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function CustomHTML(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Custom HTML" {...props} />;
}
