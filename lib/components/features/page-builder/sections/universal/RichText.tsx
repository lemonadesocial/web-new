'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function RichText(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Rich Text" {...props} />;
}
