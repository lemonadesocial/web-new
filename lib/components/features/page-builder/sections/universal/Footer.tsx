'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function Footer(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Footer" {...props} />;
}
