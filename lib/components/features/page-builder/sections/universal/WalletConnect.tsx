'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function WalletConnect(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Wallet Connect" {...props} />;
}
