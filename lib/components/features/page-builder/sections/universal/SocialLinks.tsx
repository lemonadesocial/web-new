'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function SocialLinks(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Social Links" {...props} />;
}
