'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function Spacer(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Spacer" {...props} />;
}
