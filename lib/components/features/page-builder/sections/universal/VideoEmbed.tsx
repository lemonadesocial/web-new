'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function VideoEmbed(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Video Embed" {...props} />;
}
