'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function CTABlock(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="CTA Block" {...props} />;
}
