'use client';

import { PlaceholderSection } from '../PlaceholderSection';

export function CardsGrid(props: Record<string, unknown>) {
  return <PlaceholderSection sectionLabel="Cards Grid" {...props} />;
}
