'use client';
import { UseFormReturn } from 'react-hook-form';
import { LemonHeadBodyType, LemonHeadValues } from '../types';

export function Celebrate(_props: { form: UseFormReturn<LemonHeadValues>; bodyBase: LemonHeadBodyType[] }) {
  return <>Celebrate</>;
}
