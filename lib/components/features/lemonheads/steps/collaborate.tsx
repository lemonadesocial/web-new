'use client';
import { UseFormReturn } from 'react-hook-form';
import { LemonHeadBodyType, LemonHeadValues } from '../types';

export function Collaborate(_props: { form: UseFormReturn<LemonHeadValues>; bodyBase: LemonHeadBodyType[] }) {
  return <>Collaborate</>;
}
