import { Config } from './types';

export const DEFAULT_CONFIG: Config = {
  title: 'Lemonade',
  theme: {
    styles: {
      variables: {
        // '--color-primary-500': 'red',
      },
      externals: [],
    },
  },
};
